const CACHE_NAME = "tabir26-cache-v1"; const urlsToCache = [ "/tabir26/", "/tabir26/index.html", "/tabir26/style.css", "/tabir26/script.js", "/tabir26/manifest.json" ]; self.addEventListener("install", event => { event.waitUntil( caches.open(CACHE_NAME) .then(cache => cache.addAll(urlsToCache)) ); }); self.addEventListener("fetch", event => { event.respondWith( caches.match(event.request) .then(response => response || fetch(event.request)) ); });
